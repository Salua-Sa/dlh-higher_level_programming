#!/usr/bin/python3


def square_matrix_simple(matrix=[]):
    new_matrix = []
    for row in matrix:
        ne_row = []
        for num in row:
            new_row.append(num * num)
    new_matrix.append(ne_row)
    return new_matrix
